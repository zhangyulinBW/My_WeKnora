user@123.com abc12345678

# docker
docker compose pull     # 拉取最新镜像
docker-compose --profile neo4j up -d neo4j  # 启动知识图谱
docker compose up -d   # 启动核心服务



____________________________________________________________
# 开发测试环境cd 进入目录后（非dockert镜像，不然每次修改都要重新build需要时间）
## 启动docker,此种方法同样要先启动docker镜像，因为数据在docker里，但是执行的代码是本地的
## 1、启用虚拟机平台,安装wsl,ubuntu
wsl --install 之后输入ubuntu的用户名密码
// 参考https://zhuanlan.zhihu.com/p/2023187363061342862

## 2. 在 Ubuntu 内安装开发工具
wsl -d Ubuntu  //进入Ubuntu环境

//安装开发工具（如果提示 sudo 要密码，输入你设置的ubuntu用户密码）
sudo apt update
sudo apt install -y make gcc sqlite3 libsqlite3-dev golang-go

//设置 Go 代理
go env -w GOPROXY=https://goproxy.cn,direct

//安装 air
go install github.com/air-verse/air@latest
//添加环境变量（一下两句不用改）
echo 'export PATH=$PATH:$HOME/go/bin' >> ~/.bashrc
source ~/.bashrc

## 3.使用 Air 启动（自动检测）
make dev-start
make dev-app
make dev-frontend

## 4.后续使用 make的3ge需要新的终端执行，neo4j是知识图谱
wsl -d Ubuntu
make dev-start
make dev-start DEV_ARGS=--neo4j
make dev-app
make dev-frontend

### 启动基础服务
F:
cd F:\学习\My_WeKnora
wsl -d Ubuntu
make dev-start
//make dev-stop && make dev-start 快速重启基础服务


### 启动知识图谱
F:
cd F:\学习\My_WeKnora
wsl -d Ubuntu
make dev-start DEV_ARGS=--neo4j

### 启动后端
F:
cd F:\学习\My_WeKnora
wsl -d Ubuntu
make dev-app
#### 重启，修改GO代码后 先终端按Ctrl+C 停掉它，重新运行make dev-app

### 启动前端
F:
cd F:\学习\My_WeKnora
wsl -d Ubuntu
make dev-frontend





_________________________________________________________

# 快速开发启动
## 启动基础设施
./scripts/dev.sh start

## 启动后端（新终端）
./scripts/dev.sh app

## 启动前端（新终端）
./scripts/dev.sh frontend

## 查看日志
./scripts/dev.sh logs

## 停止所有服务
./scripts/dev.sh stop

